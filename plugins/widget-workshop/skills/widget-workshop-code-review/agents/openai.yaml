interface:
  display_name: "Widget Code Review"
  short_description: "Review Widget Workshop packages before sharing or Workshop publishing."
  default_prompt: "Use $widget-workshop-code-review to review this Widget Workshop component package for manifest, locales, settings defaults, Host API calls, permissions, preview assets, security, and graceful degradation."
