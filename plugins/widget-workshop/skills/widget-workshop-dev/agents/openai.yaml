interface:
  display_name: "Widget Dev"
  short_description: "Write Widget Workshop component packages from the current creator docs."
  default_prompt: "Use $widget-workshop-dev to implement this Widget Workshop component from a source folder with manifest.json, index.html, preview assets, optional locales, optional scripts/settings.json, and validation."
